# ground_unique 第一轮：该类在图中只有一个实例，类别名本身就唯一，不需要任何修饰。
# 全是「指令式定位请求」，即 Qwen 系列 grounding 训练数据的惯用口吻 ——
# 这一句会出现在上万条样本里，只有一种写法等于让模型把它当固定口令背下来。
#! forbid: 描述 长什么样 说说 讲讲 什么颜色 是什么 有哪些 有什么
定位图中的{label}。
框出图中的{label}。
框出图中{label}的位置。
输出{label}的检测框。
请给出{label}的边界框。
找出图中的{label}，给出坐标。
{label}在图中的什么位置？
图中的{label}在哪？
把{label}框出来。
给一下{label}的坐标。
标出{label}的位置。
图里的{label}在什么位置？
