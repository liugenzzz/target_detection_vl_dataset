# 给一句现成的指代短语，让模型框出对应的目标（REC）。
# 短语是数据集里人写的，不是模型生成的 —— 这是这条路最值钱的地方。
#
# 【短语必须用「」括起来，句子用中文句号收尾】。短语多半是英文、自带英文
# 句号，直接拼在句末会让整句以 "." 结束，过不了语体闸；而且中英黏在一起
# 读不出边界，模型分不清哪部分是指令、哪部分是要匹配的描述。
#
# 【不能写「这句」「下面」「以下」】。那几个词在 register.META_WORDS 里 ——
# 那道闸是拦模型的「以下是几种写法：」这类元话语的，这里会误伤，整批样本
# 会在落盘校验被拦下。用「与……相符」绕开。
#! require-any: 框 坐标 检测 标出 定位 圈出
#! forbid: 在哪 哪里 哪儿 什么位置 描述 长什么样 什么颜色 所有 全部 每一个
#! max-len: 40
框出图中与「{expr}」相符的目标。
请在图中框出与「{expr}」一致的目标。
根据「{expr}」定位图中对应的目标。
按「{expr}」在图中标出对应的目标。
输出与「{expr}」相符的目标的检测框。
找出图中与「{expr}」相符的目标并给出坐标。
