# ground_unique 第一轮：该类在图中只有一个实例，类别名本身就唯一，不需要修饰。
#
# 【答案是坐标的问句，必须明说要框/坐标】。「这辆自行车在哪？」是宽泛问法 ——
# 到底是要一句方位描述（「在画面左下角」）还是要一个 bbox？两种答案都成立，
# 而训练数据只给了其中一种，等于在教模型猜。require-any 这一行就是这道闸。
#! require-any: 框 坐标 边界 检测 标出 定位 圈出 指出
#! forbid: 在哪 哪里 哪儿 什么位置 哪个位置 所有 全部 每一个 描述 长什么样 是什么
定位图中的{label}。
框出图中的{label}。
框出图中{label}的位置。
输出{label}的检测框。
请给出{label}的边界框。
找出图中的{label}，给出坐标。
把{label}框出来。
给出{label}的边界框坐标。
标出图中{label}的位置。
请定位{label}并给出它的坐标。
输出图中{label}的边界框。
圈出图中的{label}。
