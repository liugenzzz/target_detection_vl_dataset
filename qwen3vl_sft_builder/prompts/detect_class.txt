# 反过来盯着「别退化成问单个」：这个池子的每一句都必须带穷举词，
# 否则和 ground_unique 撞车。校验见 #! require 那一行。
# 「定位某一类的全部目标」的问法池，每行一种，构建时随机取一句。
# 全部对齐 Qwen-VL 官方 grounding 语域：框出… / 输出…检测框 / 定位… / 标出…
# 这类问法与图像内容无关，用模板池即可，不必调 VLM。
#! require-any: 所有 全部 每一个 每个 分别 逐个
#! forbid: 描述 长什么样 说说 讲讲 什么颜色 是什么 有哪些 有什么
框出图中所有的{label}。
框出图中全部的{label}。
框出画面里所有的{label}。
定位图中所有的{label}。
定位图中全部{label}的位置。
输出图中所有{label}的检测框。
输出画面中每一个{label}的检测框。
请给出图中所有{label}的边界框。
请标出图中的全部{label}。
找出图中所有的{label}，给出坐标。
把图中的{label}全部框出来。
图中所有{label}分别在什么位置？
